// Placeholder — see the staged plan. Implemented shortly; kept in the module
// list so index.html and tools/modules.js stay in step from the start.
(function (AGAT) {
  'use strict';
  AGAT.__pending = AGAT.__pending || [];
  AGAT.__pending.push('gcr140');
})(typeof globalThis !== 'undefined' && (globalThis.AGAT = globalThis.AGAT || {}));

# AGC — контейнер для Агата

*In English: [AGC.md](AGC.md).*

Иметь образ диска и знать, как его запускать, — не одно и то же. Какая машина —
Агат-7 или Агат-9, и сколько в ней памяти? И какая клавиша на клавиатуре,
посылает тот байт, которого ждёт программа? Ничего этого в `.dsk` нет.

Файл `.agc` — это одна программа и всё, что нужно для её запуска: образ диска
(со своими патчами и с тем, что программа на него записала), машина, настройки
и клавиатура.

Это JSON. Перетащите такой файл на
[эмулятор](https://amaltsev.github.io/agat-web/) — и он запустится.

Проще всего начать так: загрузить в эмулятор голый образ и нажать **Save AGC** —
получившийся контейнер — текстовый файл, который можно править.

---

## Пример

```json
{
  "agc": 1,
  "title": "RISE OUT",
  "author": "Andrew Maltsev",
  "date": "1989",
  "url": "https://github.com/amaltsev/agat-web",
  "notes": "Со звуковыми данными в исходном виде, 1989 год.",

  "machine": { "model": 7, "ram": 64 },

  "keys": {
    "KeyW": { "code": "^" }
  },
  "controls": {
    "Игра": {
      "Up Down Left Right": "Движение",
      "^": "Выстрел вправо"
    }
  },

  "media": [
    {
      "name": "rise-out.dsk",
      "data": [
        "AQIDBAUGBwgJCgsMDQ4PEBESExQVFhcYGRobHB0eHyAhIiMkJSYnKCkqKywtLi8wMTIzNDU2",
        "…"
      ],
      "patches": [ { "at": 45312, "hex": "A9 60 85 84" } ]
    }
  ]
}
```

Обязателен только `agc`, а на практике ещё и `media`: у всего остального есть
разумные значения по умолчанию, и контейнер, в котором нет ничего, кроме образа,
тоже правильный.

---

## Поля

### Что это за программа

| поле | |
|---|---|
| `agc` | версия формата — `1`. Именно по этому ключу файл и опознаётся. |
| `title` | название программы |
| `author` | автор |
| `date` | текст, а не число: `"1989"`, `"около 1985"`, `"1990-92"` |
| `url` | откуда взято или где описано |
| `notes` | происхождение, благодарности, что делает патч. Код это поле не читает. |

Часто именно эти поля — последнее место, где всё это вообще записано. Заполняйте
их.

### `machine`

| поле | |
|---|---|
| `model` | `7` или `9` |
| `ram` | **основное ОЗУ в килобайтах**: `32`, `64` или `128`. Только для Агат-7 — в Агат-9 всегда 128К. |
| `slots` | чем эта машина отличается по составу плат от штатной. Необязательное поле. |

`ram` — это ОЗУ на самой плате, а не вся память машины. И не для красоты: только
её и сканирует видеоконтроллер, и именно она маскирует поле страницы в регистре
видеорежима, то есть программа объём видит — диск, рассчитанный на 64К, на 128К
может не пойти.

Штатный Агат-7 — это **128К в трёх устройствах**: 64К основного ОЗУ, 32К ЭмПЗУ в
слоте 2 и 32К платы расширения ОЗУ в слоте 4. Агат-9 — 128К и два дисковода.
Контейнеру, которому нужна такая машина, кроме `model` и `ram` писать нечего.

#### `machine.slots`

Ключ — номер слота, `0`-`7`. В слоте, который здесь не назван, остаётся то, что
ставит штатная машина; `null` оставляет слот пустым.

```json
"machine": {
  "model": 7,
  "ram": 64,
  "slots": {
    "4": { "card": "xram", "ram": 128 },
    "2": null
  }
}
```

| поле | |
|---|---|
| `card` | `"psrom"` (ЭмПЗУ Агат-7), `"xram"` (расширение ОЗУ Агат-7), `"xram9"` (расширение ОЗУ Агат-9), `"fdd140"`, `"fdd840"` |
| `ram` | **килобайты**, для плат памяти: `16`, `32`, `48`, `64` или `128` |

`card` обязателен: запись, в которой указан один только объём, не называет
платы и не учитывается.

### Клавиатура: `keys` и `controls`

Блока два, и разница между ними в том, с какой стороны клавиатуры они
перечислены.

| | ключ | отвечает на вопрос |
|---|---|---|
| `keys` | клавиша **хоста** — `KeyW`, `Space` | что нажимать и зачем эта клавиша |
| `controls` | **код Агата** — `^`, `$5E` | что читает программа и что каждый код делает |

`keys` кладёт код Агата на физическую клавишу той клавиатуры, что лежит перед
вами, и называет рабочие клавиши программы даже там, где переназначать ничего
не нужно. `controls` клавиатуры не касается вовсе: это собственный список кодов
программы, разбитый на группы и подписанный, — его страница печатает под
экраном.

Каждый из блоков может быть и один. Только `keys` — это переназначение и доска,
прореженная до рабочих клавиш; только `controls` — панель и доска, прореженная
до названных кодов, но без переназначений. Вместе они отвечают на обе половины
одного вопроса, поэтому `#kbd=used` берёт коды из обоих.

Левые части похожи, но это разные вещи: `"Space"` — законное слово и там и там,
и означает физическую клавишу пробела в `keys` и код `$20` в `controls`.

### `keys` — клавиши, которыми пользуется программа

Клавиатура Агата — не ваша клавиатура. Программа, которая читает `^`, ждёт
`$5E`: в ЛАТ это <kbd>Shift</kbd>+<kbd>6</kbd>, а в РУС — <kbd>X</kbd>.

```json
"keys": {
  "KeyW": { "code": "^", "note": "Выстрел вправо" },
  "KeyA": "←",
  "Space": { "note": "Прыжок" },
  "ArrowUp": null
}
```

Слева — браузерный
[`KeyboardEvent.code`](https://developer.mozilla.org/docs/Web/API/UI_Events/Keyboard_event_code_values):
`KeyW`, `Digit1`, `ArrowUp`, `Space`. Он обозначает **физическую клавишу**, а не
символ, поэтому переназначение одинаково работает при любой раскладке хоста. Все
имена, которые понимает этот эмулятор, и то, что каждое из них посылает без
переназначения, собраны в
[Что посылает каждая клавиша](#что-посылает-каждая-клавиша).

Справа — либо код, который надо послать, либо `{ "code": …, "note": … }`, либо —
если кода нет вовсе — объявление, что программа пользуется клавишей **такой,
какая она есть**. `"Space": { "note": "Прыжок" }` и голое `"ArrowUp": null`
ничего не меняют в том, что эти клавиши посылают; они говорят, что клавиши
входят в число рабочих, а из этого и рисуется режим экранной клавиатуры
**Только назначенные клавиши**. У игры, которой переназначение не нужно,
управление всё равно есть — вот так оно и называется.

Код пишется одним из трёх способов:

| вид | пример |
|---|---|
| сам символ | `"^"`, `"@"`, `"Ю"` |
| шестнадцатерично | `"$5E"`, `"0x5E"` |
| по имени | `"Up"`, `"Down"`, `"Left"`, `"Right"`, `"Enter"`, `"Esc"`, `"Space"`, `"Tab"`, `"Bksp"`, `"F1"`, `"F2"`, `"F3"` |

Символами покрывается `$20`–`$7F` — это ASCII плюс кириллица Агата в порядке
КОИ-7 Н2, так что `"Ю"` и `"Ч"` пишутся как есть. Единственная ловушка — `$24`:
Агат рисует в этом месте `¤`, а не `$`, поэтому пишите `"¤"` или `"$24"`.

Переназначенная клавиша **забирается целиком**: она посылает свой код в обеих
раскладках и с РЕГ и УПР и без них. Клавиша управления, меняющая смысл оттого,
что игрок держит модификатор, была бы хуже, чем отсутствие переназначения. То,
что клавиша посылала раньше, пока контейнер загружен, недоступно — поэтому
занимайте клавиши, которые программе больше не нужны.

`note` говорит, что клавиша **делает**. Это и есть та половина, ради которой всё
затевалось: экранная клавиатура показывает эту подпись, и при наведении на `^`
читается **`W (Выстрел вправо)`** вместо того, чтобы человек это выяснял сам.

Ради прореженной доски и стоит перечислять все рабочие клавиши,
переназначенные и нет: это клавиши машины, где каждая, до которой не достаёт ни
одна из перечисленных, сжата в полоску, — оставшиеся стоят там же, где их держит
Агат. Доска состоит из трёх областей, которые сворачиваются независимо: основная
клавиатура, стрелки и цифровой блок. Программе, которая не пользуется цифровым
блоком, его и не покажут, а одна названная стрелка выводит весь крест целиком.
Собственные клавиши доски — СБР, УПР, РУС/LAT, РЕГ — не рисуются вовсе: это не
клавиши программы, а на телефоне они занимали почти весь экран.

В меню этот режим доступен только для контейнера, который называет клавиши или
управление, и на телефоне такой контейнер с него и открывается.
`node tools/check.js keys <файл.agc>` рисует ту же доску в терминале.

### `controls` — что читает программа и зачем

```json
"controls": {
  "Игра": {
    "Up Down Left Right": "Движение",
    "Space": "Стоп",
    "^": "Выстрел вправо"
  },
  "Читы": { "K": "Самоубийство", "К": "Конец игры" }
}
```

Группы — в том порядке, в каком их перечисляет файл, строки — в том, в каком их
перечисляет группа. Ключ строки — это **один или несколько кодов через пробел**,
записанных любым из способов [выше](#keys--клавиши-которыми-пользуется-программа),
так что крест стрелок — одна строка, а не четыре, и `"Space Enter"` — одна строка
для программы, которой годится любая. Значение — что эта строка **делает**;
`true` означает управление, которое стоит назвать, но добавить к нему нечего.

**Коды, и никаких сочетаний.** Клавиатура Агата — шифратор, кладущий в `$C000`
один байт. РЕГ прибавляет `$20` по всему буквенному блоку — оттого на клавишах и
по две надписи, — так что то, что человек называет РЕГ+К, есть единственный код
`$6B`, записываемый как `"К"` или `"$6B"`. УПР сворачивается точно так же в
`$81`–`$9F`. Значить здесь `+` нечему, и он не принимается.

Этот блок читают трое:

- **Панель управления** под экраном: карточка групп, поставленных рядом. Она
  намеренно неподвижна. На ней написано, что читает *программа* — `Q` есть `$51`
  при любой раскладке, — а доска рядом отвечает на вторую половину вопроса: какая
  клавиша хоста достаёт до этого кода сейчас. Единственная клавиша хоста на
  панели — переназначение контейнера, `^ (W)`: оно держится во всех плоскостях и
  потому единственное не съезжает при ЛАТ/РУС.
- **Прореженная доска**: она берёт коды отсюда вместе с теми, до которых достают
  клавиши из `keys`.
- **Меню клавиатуры**: в нём появляется по строке на группу, и доску можно
  урезать до той части игры, которая сейчас в руках. Выбор едет в адресе:
  `#kbd=used%3AЧиты`. Нажатие на группу прямо на панели выбирает то же самое
  пальцем, а нажатие на ту, что уже показана, возвращает все.

Две ловушки, о которых стоит знать:

- **Не называйте группу или управление голой цифрой.** Объекты JSON обходятся с
  целочисленными ключами первыми, что бы ни говорил файл, так что управление,
  записанное как `"1"`, прыгнет в начало своей группы. Пишите `"$31"`.
- **Два управления могут попасть на одну клавишу.** `K` и `К` — нижняя и верхняя
  половины одной клавиши Агата, так что контейнер, назвавший обе, получает на
  доске одну клавишу. Она рисуется с подчёркнутыми обеими надписями и называет
  оба управления во всплывающей подсказке, а на доске появляется клавиша
  **РЕГ** — единственная служебная, которую она вообще рисует, — потому что без
  регистра до верхней половины касанием не добраться. Нажмите РЕГ, затем
  клавишу. Регистр одноразовый, как и должно быть при одном пальце, и появляется
  только тогда, когда он кому-то нужен.

`node tools/check.js keys <файл.agc>` печатает панель и доску вместе, а
`--group=ИМЯ` урезает их так же, как это делает меню.

### `media`

Список, загружается по порядку. Диски попадают в тот дисковод, который их
прочтёт; `.fil` кладётся прямо в память.

| поле | |
|---|---|
| `name` | исходное имя файла. **Формат определяется по размеру, а не по нему** — образы Агата в природе сплошь и рядом названы неправильно. |
| `data` | base64 строками |
| `patches` | изменения, применяемые после раскодирования, по порядку; hex или base64 |

`data` — обычный base64, без сжатия: контейнер остаётся текстовым файлом, внутрь
которого можно заглянуть любыми привычными средствами. Строки по 76 символов —
это 57 байт и целое число групп base64, поэтому каждая строка самостоятельна.
При чтении принимается и одна длинная строка, и строки любой другой длины.

Контейнер несёт всё, что понимает эмулятор: `.aim`, `.dsk` и `.nib` на 140К и
840К, а также программы `.fil`. Контейнер внутри контейнера не допускается.

### `patches`

```json
"patches": [ { "at": 45312, "hex": "A9 60 85 84" },
             { "at": 46080, "data": ["…", "…"] } ]
```

`at` — смещение в байтах от начала раскодированных данных, а что туда записать,
говорит либо `hex` — пробелы и запятые разрешены, так что байты можно
сгруппировать так, как они читаются, — либо `data`, base64 строками, как и сами
данные. Читатель принимает и то и другое. Запись, где есть и `hex`, и `data`, —
ошибка, а не выбор, который надо как-то разрешить.

Пишущий выбирает по размеру: до 32 байт — hex, больше — base64. Мелкая правка
остаётся тем, что можно прочесть, а переписанный сектор не стоит трёх символов
на байт.

Данные остаются **образом ровно в том виде, в каком он был найден**, а изменения
живут здесь. В этом весь смысл разделения: контейнер несёт нетронутую копию
того, откуда он взялся, изменение видно всякому, кто откроет файл, а сохранение
загруженного контейнера пишет обратно то, что в нём лежало, а не пропатченный
результат.

**То, что программа записала на диск, сохраняется точно так же.** Снятый с
блокировки дисковод 140К пишет в лежащий в памяти поток нибблов; при сохранении
каждая записанная дорожка читается обратно в те 16 секторов, из которых была
собрана, а разница попадает сюда. Игра, хранящая рекорд, обходится патчем в
несколько сотен байт — по правилу выше это один блок base64, — а не второй
копией диска.

Дорожка, которая не читается обратно секторами — диск, отформатированный иначе,
или запись, оборванная на середине, — не имеет секторного образа, разницей от
которого мог бы стать патч. Тогда сохраняется весь поток нибблов: данные `.nib`
и никаких патчей. Файл получается больше, но ничего не теряет и загружается без
каких-либо дополнительных сведений — носители опознаются по размеру.

---

## Как сделать контейнер

### Из эмулятора

**Save AGC** записывает контейнер по состоянию машины: что вставлено в
дисководы, модель и объём памяти, действующее переназначение клавиш и всё,
что программа записала на снятый с блокировки диск. Ничего не спрашивает.

Контейнер, загруженный из файла, сохраняет своё название и имя файла. Сделанный
из голого образа берёт имя образа и туда, и туда: `game.dsk` сохранится как
`game.agc` с названием `game.dsk`. Переименуйте и откройте в текстовом
редакторе, чтобы дописать автора, дату и клавиши.

### Из командной строки

```sh
node tools/mkagc.js game.dsk \
  --title="…" --author="…" --date=1989 --url=https://… \
  --model=7 --ram=64 \
  --key="KeyW:^:Выстрел вправо" > game.agc
```

`--key` — это `КЛАВИША:КОД:ПОДПИСЬ`, разбирается по первым двум двоеточиям, и
может повторяться. `--patch=СМЕЩЕНИЕ:HEX` задаёт патч напрямую, а
`--diff=<изменённый образ>` вычисляет патчи сам, сравнив изменённую копию с
исходной, — обычно патч именно так и получается.

---

## Как загрузить

Перетащите файл на страницу, откройте через **Open…** или назовите его в адресе:

    index.html#agc=examples/rise-out.agc

Вариант с адресом файл загружает по сети, поэтому нужна страница, отданная
сервером: на `file://` `fetch` запрещён и остаётся **Open…**. Остальные ключи
адреса входят в ту машину, которую собирает контейнер, а не накладываются
поверх неё, так что `#agc=…&model=9` пробует программу на другой машине, ничего
не правя в файле, — и запускается она именно на этой другой машине.

Все консольные инструменты принимают контейнер везде, где принимают образ, и
запускают его на той машине, которую он называет:

```sh
node tools/check.js sniff game.agc    # что за файл и что в нём написано
node tools/check.js boot  game.agc    # загрузить и доложить, докуда дошло
node tools/shot.js        game.agc    # загрузить и записать экран в PNG
```

---

## Что посылает каждая клавиша

Любое имя из таблиц ниже принимается слева в `keys`; всё остальное игнорируется,
и строка состояния скажет, что именно. В столбцах — то, что клавиша посылает,
**пока она не переназначена**, и это же будет дальше посылать клавиша,
объявленная как есть.

В каждой клетке — начертание, которое рисует Агат, и сам код, записанный так,
как его принимают `keys` и `controls`. В `$C000` седьмой бит всегда взведён, так
что `$40` и `$C0` — одна и та же клавиша; в таблицах стоит семибитная запись
всюду, где к ней есть начертание.

Две половины буквы — один байт в двух наборах символов: РЕГ прибавляет ровно
`$20` по всему блоку, и ASCII `@A-Z[\]^_` переезжает в кириллицу Агата в порядке
КОИ-7 Н2. Поэтому обе подписи умещаются на одной клавише, и поэтому `Ч` — это
РУС <kbd>X</kbd> и ничто в ЛАТ.

Коды без начертания записаны шестнадцатерично — так же, как их принимает `keys`.
`—` значит, что таблица не отображает клавишу никуда: `Insert`, `Delete` и
`F4`-`F12` можно занимать свободно, терять нечего. `Backspace` и `ArrowLeft`
посылают один и тот же `$88` — у машины одна `←`.

**Буквы — зависят от ЛАТ/РУС и РЕГ**

| `code` | ЛАТ | ЛАТ+РЕГ | РУС | РУС+РЕГ |
|---|---|---|---|---|
| `KeyQ` | Q $51 | Я $71 | J $4A | Й $6A |
| `KeyW` | W $57 | В $77 | C $43 | Ц $63 |
| `KeyE` | E $45 | Е $65 | U $55 | У $75 |
| `KeyR` | R $52 | Р $72 | K $4B | К $6B |
| `KeyT` | T $54 | Т $74 | E $45 | Е $65 |
| `KeyY` | Y $59 | Ы $79 | N $4E | Н $6E |
| `KeyU` | U $55 | У $75 | G $47 | Г $67 |
| `KeyI` | I $49 | И $69 | [ $5B | Ш $7B |
| `KeyO` | O $4F | О $6F | ] $5D | Щ $7D |
| `KeyP` | P $50 | П $70 | Z $5A | З $7A |
| `KeyA` | A $41 | А $61 | F $46 | Ф $66 |
| `KeyS` | S $53 | С $73 | Y $59 | Ы $79 |
| `KeyD` | D $44 | Д $64 | W $57 | В $77 |
| `KeyF` | F $46 | Ф $66 | A $41 | А $61 |
| `KeyG` | G $47 | Г $67 | P $50 | П $70 |
| `KeyH` | H $48 | Х $68 | R $52 | Р $72 |
| `KeyJ` | J $4A | Й $6A | O $4F | О $6F |
| `KeyK` | K $4B | К $6B | L $4C | Л $6C |
| `KeyL` | L $4C | Л $6C | D $44 | Д $64 |
| `KeyZ` | Z $5A | З $7A | Q $51 | Я $71 |
| `KeyX` | X $58 | Ь $78 | ^ $5E | Ч $7E |
| `KeyC` | C $43 | Ц $63 | S $53 | С $73 |
| `KeyV` | V $56 | Ж $76 | M $4D | М $6D |
| `KeyB` | B $42 | Б $62 | I $49 | И $69 |
| `KeyN` | N $4E | Н $6E | T $54 | Т $74 |
| `KeyM` | M $4D | М $6D | X $58 | Ь $78 |

**Цифры и знаки — зависят от ЛАТ/РУС и РЕГ**

| `code` | ЛАТ | ЛАТ+РЕГ | РУС | РУС+РЕГ |
|---|---|---|---|---|
| `Digit1` | 1 $31 | ! $21 | 1 $31 | ! $21 |
| `Digit2` | 2 $32 | @ $40 | 2 $32 | " $22 |
| `Digit3` | 3 $33 | # $23 | 3 $33 | # $23 |
| `Digit4` | 4 $34 | ¤ $24 | 4 $34 | ; $3B |
| `Digit5` | 5 $35 | % $25 | 5 $35 | % $25 |
| `Digit6` | 6 $36 | ^ $5E | 6 $36 | : $3A |
| `Digit7` | 7 $37 | & $26 | 7 $37 | ? $3F |
| `Digit8` | 8 $38 | * $2A | 8 $38 | * $2A |
| `Digit9` | 9 $39 | ( $28 | 9 $39 | ( $28 |
| `Digit0` | 0 $30 | ) $29 | 0 $30 | ) $29 |
| `Minus` | - $2D | _ $5F | - $2D | - $2D |
| `Equal` | = $3D | + $2B | = $3D | + $2B |
| `BracketLeft` | [ $5B | Ш $7B | H $48 | Х $68 |
| `BracketRight` | ] $5D | Щ $7D | _ $5F | Ъ $7F |
| `Semicolon` | ; $3B | : $3A | V $56 | Ж $76 |
| `Quote` | ' $27 | " $22 | \ $5C | Э $7C |
| `Backquote` | @ $40 | ^ $5E | @ $40 | ^ $5E |
| `Backslash` | \ $5C | Э $7C | \ $5C | Э $7C |
| `Comma` | , $2C | < $3C | B $42 | Б $62 |
| `Period` | . $2E | > $3E | @ $40 | Ю $60 |
| `Slash` | / $2F | ? $3F | . $2E | , $2C |

**Правка — один код при любой раскладке**

| `code` | посылает | `code` | посылает |
|---|---|---|---|
| `Escape` | Esc $9B | `Enter` | ↵ $8D |
| `Backspace` | ← $88 | `Space` | пробел $20 |
| `Tab` | Tab $89 |  |  |

**Стрелки и блок навигации — один код при любой раскладке**

| `code` | посылает | `code` | посылает |
|---|---|---|---|
| `ArrowUp` | ↑ $99 | `End` | $8A |
| `ArrowLeft` | ← $88 | `PageUp` | ↑ $99 |
| `ArrowRight` | → $95 | `PageDown` | ↓ $9A |
| `ArrowDown` | ↓ $9A | `Insert` | — |
| `Home` | $8B | `Delete` | — |

**Функциональные клавиши — один код при любой раскладке**

| `code` | посылает | `code` | посылает |
|---|---|---|---|
| `F1` | F1 $84 | `F7` | — |
| `F2` | F2 $85 | `F8` | — |
| `F3` | F3 $86 | `F9` | — |
| `F4` | — | `F10` | — |
| `F5` | — | `F11` | — |
| `F6` | — | `F12` | — |

**Цифровой блок — один код при любой раскладке**

| `code` | посылает | `code` | посылает |
|---|---|---|---|
| `NumpadMultiply` | * $2A | `NumpadAdd` | + $2B |
| `Numpad7` | $90 | `Numpad1` | $9D |
| `Numpad8` | $91 | `Numpad2` | $9E |
| `Numpad9` | $92 | `Numpad3` | $9F |
| `NumpadSubtract` | - $2D | `Numpad0` | $81 |
| `Numpad4` | $93 | `NumpadDecimal` | $82 |
| `Numpad5` | $94 | `NumpadEnter` | $83 |
| `Numpad6` | $9C | `NumpadDivide` | / $2F |

---

## Если вы пишете свою реализацию

- Версия — это `agc`. Файл с неизвестной версией лучше отвергнуть, чем угадывать.
- Опознавайте контейнер по ключу `agc`, а не по расширению.
- Незнакомые поля надо сохранять, а не выбрасывать. Контейнеры часто правят
  руками, и читатель, молча теряющий непонятое, рано или поздно съест чьи-то
  записи.
- `ram` — в килобайтах, `date` — строка. И то и другое легко переврать во второй
  реализации.
- В патче есть либо `hex`, либо `data`, но никогда не оба. Читайте любой из них,
  а запись с обоими отвергайте.

Эталонная реализация — [`src/agc.js`](src/agc.js): около 200 строк, без
зависимостей, чтение и запись в одном файле.

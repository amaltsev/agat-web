* Implement an .agc format - Agat Container, binary as base64, binary patches (if any), title, machine hardware, quirks. All as JSON.
* Keyboard remap (also make it a part of .agc). I.e. ^ to w for Rise Out
* Check & implement undocumented CPU commands
* On-screen keyboard - Agat's keyboard and its state as it is read by the emulator. In many games it's unclear how PC keys map to what the game expects. Show PC keypresses on that keyboard as well, to see what maps to what.

* Implement an .agc format - Agat Container, binary as base64, binary patches (if any), title, machine hardware, quirks. All as JSON.
* Keyboard remap (also make it a part of .agc). I.e. ^ to w for Rise Out
* Check & implement undocumented CPU commands
* On-screen keyboard: a "only mapped keys" mode, once .agc can say which keys a game uses. The full АГАТ and PC boards are in, and both light the cap a host keypress reaches.
* Better phone screen support, with on screen buttons for games that have them mapped in .agc, and an option to use the native phone keyboard or on-screen full keyboard otherwise.
